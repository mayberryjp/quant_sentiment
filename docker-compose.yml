services:
  api:
    build: .
    environment:
      DATABASE_URL: postgresql+psycopg://sentiment:sentiment@db:5432/sentiment
      API_LISTEN_ADDRESS: 0.0.0.0
      API_PORT: "8017"
    ports:
      - "8017:8017"
